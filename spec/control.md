
# Control Flow
## If

### If
?{(cond) body }

### If with Else If
?{(cond) body }?{(cond) body }.....

### If with else
?{(cond) body }?{ body }

### If with else if and else
?{(cond) body }?{(cond) body }?{ body }

## While
??{(cond) body }
