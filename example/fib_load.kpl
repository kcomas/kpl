
// Load Fibonacci Function and run it in a loop

fib_import :: "./fib.kpl"\load

total :: Const[U64] $ 30

values :: { I64`_; U64`idx; fib(I64 $ idx + 1) } @ Array[I64] { total }

values @ { I64`n; U64`idx; print_fib(idx; n) }
