
// Load Fibonacci Function and map the values into an array

 "./fib.kpl"\import [ `fib; `print_fib ]

total : Const[U64] $ 31

values : { `_; `idx; fib(I64 $ idx) } @ Array[I64; total] ()

values @ { `n; `idx; print_fib(idx; n) }
